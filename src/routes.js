export const routes = {
    login: '/login',
    welcome: '/welcome',
    register: '/register',
    dashboard: '/dashboard',
    index: '/'
}

export const authRoutes = [routes.login, routes.register, routes.welcome];
